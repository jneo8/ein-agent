# How to deploy canonical identity platform

The deployment follow the instruction on https://charmhub.io/topics/canonical-identity-platform/tutorials/e2e-tutorial
But switch the version to `v1.0.0`

```sh
git clone https://github.com/canonical/iam-bundle-integration.git && cd iam-bundle-integration
git checkout v1.0.0
```
